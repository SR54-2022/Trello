apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: eventstore-data
  name: eventstore-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
