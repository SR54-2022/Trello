apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: namenode-data
  name: namenode-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
